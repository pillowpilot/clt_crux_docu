{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TicketClosed",
  "type": "object",
  "properties": {
    "type": {
      "const": "v0.agents.ticket_closed"
    },
    "uuid": {
      "type": "string",
      "format": "uuid"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "ticket_id": {
      "type": "string"
    },
    "conversation_id": {
      "type": "string"
    }
  },
  "required": ["type", "uuid", "timestamp", "ticket_id", "conversation_id"]
}

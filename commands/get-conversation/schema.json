{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "GetConversation",
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string",
      "format": "uuid"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "conversation_id": {
      "type": "string"
    },
    "start_timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "end_timestamp": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["uuid", "timestamp", "conversation_id"]
}
